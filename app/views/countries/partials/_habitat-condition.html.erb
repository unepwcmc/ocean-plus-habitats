<section class="section-padding bg--navy-dark white">
  <div class="container">
    <div class="h2-wrapper flex flex-v-center">
      <h2><%= t('countries.shared.habitat_condition.title') %></h2>
      <modal-trigger id="citation" :modal-content="<%= modal_content %>" class="icon--citation margin-space-left"></modal-trigger>
    </div>

    <% t('countries.shared.habitat_condition.text').each do |p| %>
      <p class="text-body"><%= p %></p>
    <% end %>
  </div>
</section>