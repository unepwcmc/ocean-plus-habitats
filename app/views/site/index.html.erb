<%= render '/global/header-home' %>

<%= render '/site/partials/map' %>

<%= render partial: '/site/partials/habitat-change', locals: { modal_content: @habitat_change_modal } %>

<%= render partial: '/site/partials/red-list', locals: { modal_content: @red_list_modal } %>

<%= render partial: '/site/partials/habitat-cover', locals: { habitat_cover: @habitat_cover, modal_content: @habitat_cover_modal } %>

<%= render partial: '/site/partials/sdg', locals: { doughnut_chart: @doughnut_chart.to_json } %>

<modal id="citation"></modal>